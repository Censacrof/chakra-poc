import { Grid, GridItem } from "@chakra-ui/react";
import { FC } from "react";

export const Console: FC = () => {
  return (
    <Grid flexGrow={1} alignSelf={"stretch"}>
      <GridItem>asd</GridItem>
    </Grid>
  );
};
